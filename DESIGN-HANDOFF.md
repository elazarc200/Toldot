# ניסוי עיצוב — תשתית ודף הבית

העותק הזה נוצר מהפרויקט המקומי Toldot, בענף design/homepage-experiment. הפרויקט המקורי לא נערך ולא נדחפו שינויים ל-GitHub.

## מה נבנה
- דף בית לפי האפיון: מפה, חיפוש, שלושה שערי ניווט, באנר עריכתי אחד ופוטר.
- גופן Heebo מקומי, צבעי שנהב וכחול, משתני עיצוב משותפים ורכיבי סמלים.
- ניווט למחשב ותפריט נפתח למובייל; קישור דילוג לתוכן, מיקוד מקלדת ותמיכה בהפחתת תנועה.
- קווי החוף והאגמים מבוססים על Natural Earth, ללא גבולות מדיניים או טענות על קשרים היסטוריים.

## גבולות הניסוי
הבאנר העריכתי מציג תוכן קבוע לדוגמה. לא נוספה מערכת לתזמון או בחירת תוכן. נקודות המפה מפנות לחיפוש בשם המקום. ״חכמים ואישים״ מפנה לאינדקס האלפביתי הקיים; עיצוב ספריית אישים עצמאית נשאר לשלב הבא.

קובצי סודות והגדרות מסד הנתונים לא הועתקו. דף הבית פועל בלי חיבור ל-Supabase; חיפוש נתונים ודפי תוכן דורשים את הגדרות הסביבה הקיימות שלך. לא שונו מסד הנתונים, הרשאות, תהליך הפרסום או ממשקי הנתונים.

## הפעלה והמשך ב-Cursor
אפשר לפתוח את התיקייה הזאת ב-Cursor. התקנת התלויות: npm ci. הפעלה מקומית: npm run dev. לפני בדיקת דפי הנתונים יש להגדיר את ערכי הסביבה לפי README.md ו-.env.example.

כל השינויים ניתנים להעברה באמצעות Git. יש לבדוק את דף הבית במחשב ובמובייל ולחבר תוכן עריכתי מאושר לפני שילוב בגרסה פעילה. השינויים לא פורסמו לאינטרנט.

## מקורות נכסים
- Natural Earth: https://www.naturalearthdata.com/about/terms-of-use/ — נתוני קו חוף ואגמים ברשות הציבור. ההטלה משמשת להתמצאות בלבד.
- Heebo: Google Fonts, SIL Open Font License. רישיון מצורף ב-public/fonts/OFL.txt.

## בדיקות וחיבור התצוגה
89 בדיקות קיימות עברו. הקוד עבר הידור ובדיקת טיפוסים. בדיקת הבנייה המלאה דורשת Supabase בגלל עמודי הנתונים הקיימים. לתצוגה בחלונית הוזנו הגדרות הגישה הציבורית מהפרויקט המקורי לזיכרון התהליך בלבד; קובץ ההגדרות לא הועתק לעותק הזה. בהרצה עצמאית מ-Cursor יש להגדיר אותן לפי ההוראות למעלה.

התאמות מובייל ונגישות נכללו בקוד. טרם בוצעה בדיקה חזותית בדפדפנים או בדיקת נגישות מלאה, ולכן אין כאן אישור עמידה ב-WCAG.
`nתוצאה סופית: הבנייה המלאה (npm run build) הסתיימה בהצלחה עם הגדרות הגישה הציבורית הקיימות.
